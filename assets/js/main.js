/**
 * ==========================================
 * Sleep Insight
 * main.js
 * 应用入口文件
 * ==========================================
 */

import { App } from "./core/app.js";

/**
 * 页面加载完成以后启动应用
 */
document.addEventListener("DOMContentLoaded", () => {

    App.init();

});